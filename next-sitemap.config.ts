// next-sitemap.config.js
/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://quick-convert-img.vercel.app/',
  generateRobotsTxt: true,
  // Optional: exclude specific paths if needed
  // exclude: ['/admin/**'],
};
